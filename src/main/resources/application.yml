server:
  port: 9091
  error:
    whitelabel:
      enabled: false

spring:
  profiles:
    active: local
  jpa:
    properties:
      hibernate:
        show-sql: true
        format_sql: true
#    database-platform: org.hibernate.dialect.OracleDialect
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.OracleDialect